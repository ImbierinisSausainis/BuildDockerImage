# Use an appropriate Java base image (OpenJDK 17)
FROM openjdk:17-jdk-slim

# Set up the working directory inside the container
WORKDIR /app

# Expose the necessary port (use 8080 for your Java Web App)
EXPOSE 8080
